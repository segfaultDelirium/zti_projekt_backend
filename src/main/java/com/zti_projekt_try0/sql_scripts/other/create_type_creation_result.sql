CREATE TYPE zti_projekt2.creation_result AS (
                                                success BOOLEAN,
                                                message TEXT,
                                                created_record_id integer
                                            );